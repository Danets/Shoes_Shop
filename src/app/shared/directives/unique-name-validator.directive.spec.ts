import { AsyncUniqueNameValidatorDirective } from './async-unique-name-validator.directive';

describe('AsyncUniqueNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncUniqueNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
