import { MatchPassordDirective } from './match-passord.directive';

describe('MatchPassordDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchPassordDirective();
    expect(directive).toBeTruthy();
  });
});
